# Pokédex Dashboard

Frontend React + Vite que consume el módulo `pokemon` del backend NestJS.

## Setup

```bash
# Desde esta carpeta
npm install
npm run dev
```

El dashboard corre en http://localhost:5173

> Requiere que el backend (`plataforma-api`) esté corriendo en el puerto 3001.
